# PyQueue UI Configuration
# Port for the Vite development server
VITE_UI_PORT=3000

# PyQueue Server API Configuration
VITE_API_HOST=localhost
VITE_API_PORT=8000
VITE_API_URL=http://localhost:8000

# Optional: API Key for authentication (if needed in the future)
VITE_API_KEY=
